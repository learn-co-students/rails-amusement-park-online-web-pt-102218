<%=@attraction.name%>

<%=hidden_field(:attraction, :id, :value => params[:id]) %>

<%= button_to "Go on this ride", ride_path(@attraction)%>

<%if @current_user && @current_user.admin %>
    <%=link_to "Edit Attraction", edit_attraction_path(@attraction)%>
<%end%>