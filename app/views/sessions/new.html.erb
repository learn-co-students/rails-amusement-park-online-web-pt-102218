<%= form_for @user, method: :post, url: signin_path do |f| %>
    <%= f.label :name %>
    <%= f.collection_select(:name, User.all, :name, :name, include_blank: true) %>
    <%= f.label :password %>
    <%= f.password_field :password, id:"password" %>
    <%= f.submit 'Sign In'%>
<%end%>