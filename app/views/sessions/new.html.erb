<%= form_for @user, url: signin_path, method: :post  do |f| %> 
    <%= f.collection_select(:name, User.all, :id, :name , include_blank: true) %> 
    <%= f.password_field :password, {:autocomplete => "off"} %> 
    <%=f.submit%> 
<%end%> 